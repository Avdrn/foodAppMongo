
<div class="movies">
  {{#each movies}}
    <div class="each-movie">
      <img src="{{this.image}}" alt="movie-img"> 
      <h1>{{this.title}}</h1>
      <a href="/movie?id={{this._id}}">See more</a>  
    </div>    
  {{/each}}
</div>